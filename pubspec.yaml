name: todo_list
description: "App that makes everydays duties easier du to the fact planning"

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  shared_preferences:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^2.24.0
  firebase_auth: ^4.15.0
  cloud_firestore: ^4.13.0

  # Google / Apple Sign-In
  google_sign_in: ^6.1.5
  sign_in_with_apple: ^5.0.0

  # Localization
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

  # File storage
  path_provider: ^2.0.15

  # Utilities
  uuid: ^4.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: true
  uses-material-design: true
